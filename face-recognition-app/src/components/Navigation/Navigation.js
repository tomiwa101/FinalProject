import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}} >
            <p className='link right fw4 db black dim pointer'>Sign Out</p>
        </nav>
    );
}

export default Navigation;